[project]
name = "moyi-edge"
version = "0.1.0"
description = "Python SDK helpers for the MoYi edge translation runtime"
requires-python = ">=3.10"
dependencies = []

[project.scripts]
moyi-edge = "moyi_edge.cli:main"

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"
